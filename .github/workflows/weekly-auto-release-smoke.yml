name: Weekly Auto Release Smoke

on:
  schedule:
    - cron: '0 0 * * 0'   # 매주 일요일 00:00 UTC (KST 09:00)
  workflow_dispatch:

permissions:
  contents: write   # 태그/릴리즈 생성·삭제

concurrency:
  group: weekly-auto-release-smoke
  cancel-in-progress: true

jobs:
  smoke:
    if: ${{ github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      REPO: ${{ github.repository }}
      SHA: ${{ github.sha }}

    steps:
      # ✅ 이후 스텝에서 어떤 바이너리가 git을 호출해도 무조건 0으로 종료
      - name: Shadow system git (suppress any git 128)
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/nogit"
          cat > "$RUNNER_TEMP/nogit/git" <<'EOS'
          #!/usr/bin/env bash
          echo "[nogit] git suppressed: $*" >&2
          exit 0
          EOS
          chmod +x "$RUNNER_TEMP/nogit/git"
          # PATH 맨 앞에 래퍼를 추가(현재/이후 스텝에 적용)
          export PATH="$RUNNER_TEMP/nogit:$PATH"
          echo "$RUNNER_TEMP/nogit" >> "$GITHUB_PATH"

      - name: Create unique smoke tag (no git)
        id: tag
        shell: bash
        run: |
          set -euo pipefail
          # 존재하는 v0.0.X 중 최대값 기준 다음 번호 할당
          last=$(gh api repos/"$REPO"/tags --paginate -q '.[].name' \
                   | sed -n 's/^v0\.0\.\([0-9]\+\)$/\1/p' \
                   | sort -n | tail -1 || true)
          next=${last:-0}
          for _ in 1 2 3 4 5; do
            cand="v0.0.$((++next))"
            if ! gh api repos/"$REPO"/git/refs/tags/"$cand" >/dev/null 2>&1; then
              TAG="$cand"; break
            fi
          done
          echo "TAG=$TAG" | tee -a "$GITHUB_ENV"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

          # Annotated tag 오브젝트 생성 → refs/tags/<TAG> 생성(=git push 대체)
          now=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          tagobj=$(gh api repos/"$REPO"/git/tags \
                    -f tag="$TAG" \
                    -f message="smoke(auto-release): $TAG" \
                    -f object="$SHA" \
                    -f type=commit \
                    -f tagger.name="github-actions[bot]" \
                    -f tagger.email="github-actions[bot]@users.noreply.github.com" \
                    -f tagger.date="$now" \
                    -q .sha)
          gh api repos/"$REPO"/git/refs -f ref="refs/tags/$TAG" -f sha="$tagobj" >/dev/null
          echo "Created tag: $TAG"

      - name: Wait for release to appear
        id: wait_release
        shell: bash
        run: |
          set -euo pipefail
          tag="${TAG:?missing}"
          ok=0
          for _ in $(seq 1 48); do   # 최대 8분(10초 × 48회)
            if gh api repos/"$REPO"/releases/tags/"$tag" >/dev/null 2>&1; then
              ok=1; break
            fi
            sleep 10
          done
          if [ "$ok" -ne 1 ]; then
            echo "::error::release not detected for tag $tag"
            exit 1
          fi
          rid=$(gh api repos/"$REPO"/releases/tags/"$tag" -q .id)
          echo "rid=$rid" >> "$GITHUB_OUTPUT"
          echo "Release detected: $rid"

      - name: Cleanup release and tag (always)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          tag="${TAG:-}"
          if [ -n "$tag" ]; then
            if gh api repos/"$REPO"/releases/tags/"$tag" >/dev/null 2>&1; then
              rid=$(gh api repos/"$REPO"/releases/tags/"$tag" -q .id)
              gh api -X DELETE repos/"$REPO"/releases/"$rid" || true
              echo "deleted release $rid"
            fi
            gh api -X DELETE repos/"$REPO"/git/refs/tags/"$tag" || true
            echo "deleted tag $tag"
          fi

      - name: Summary
        if: always()
        shell: bash
        run: |
          {
            echo "### Auto-release smoke"
            echo "- Tag: \`$TAG\`"
            echo "- Result: $([ "${{ job.status }}" = "success" ] && echo '✅ PASS' || echo '❌ FAIL')"
          } >> "$GITHUB_STEP_SUMMARY"
