export const el=(s,r=document)=>r.querySelector(s);export function setText(id,v){const n=document.getElementById(id);if(n)n.textContent=String(v)}export function setBadge(t){const a=document.getElementById("kobong-status-inline");if(a)a.textContent=t;const b=document.getElementById("kobong-status");if(b)b.textContent=t}export function setSummary(o){for(const k in o){const n=document.getElementById("sum-"+k);if(n)n.textContent=String(o[k])}}export function setChip(id,state,text){const n=document.getElementById(id);if(!n)return;n.className="kb-chip "+(state||"muted");if(text)n.textContent=text}export function prependEvent(txt,cls="muted"){const ul=document.getElementById("events");if(!ul)return;const li=document.createElement("li");li.textContent=txt;li.className=cls;ul.prepend(li);while(ul.childElementCount>100){ul.removeChild(ul.lastElementChild)}}