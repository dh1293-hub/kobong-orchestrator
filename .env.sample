# Logging
LOG_MAX_BYTES=5242880
LOG_BACKUPS=5
LOG_RETENTION_DAYS=14

# Alerts
ALERT_MIN_LEVEL=ERROR
ALERT_SINK=console

# Release notes backend (acl|gitlog|ps) - default: acl
REL_NOTES_BACKEND=acl
# --- KOBONG API ---
KOBONG_API_ENABLED=false
# KOBONG_API_TOKEN=<put token if required by upstream API>
