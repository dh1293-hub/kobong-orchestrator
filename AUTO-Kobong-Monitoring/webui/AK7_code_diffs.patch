--- /mnt/data/ak7_src/webui/AUTO-Kobong-Monitoring-Han.html
+++ /mnt/data/ak7_patched/webui/ak7-monitoring.html
@@ -4,7 +4,7 @@
   <meta charset="utf-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1" />
   <meta http-equiv="x-ua-compatible" content="ie=edge" />
-  <title>Orchestrator‑Monitoring — UI</title>
+  <title>AK7 Monitoring — UI</title>
   <!-- xterm 정적 자산(있으면 사용). 없으면 UI만 표시되고 Shell은 안내 메시지로 대체됩니다. -->
   <link rel="stylesheet" href="./public/xterm/xterm.css">
   <style>
@@ -155,7 +155,7 @@
 
     <!-- ===== Aside ===== -->
     <aside class="aside" role="navigation" aria-label="기능 탭">
-      <div class="project">Orchestrator‑Monitoring</div>
+      <div class="project">AK7 Monitoring</div>
       <button class="navbtn" data-orchmon-tab="overview" aria-selected="true">Overview<span>⟶</span></button>
       <button class="navbtn" data-orchmon-tab="timeline">Timeline<span>⟶</span></button>
       <button class="navbtn" data-orchmon-tab="messages">Messages<span>⟶</span></button>
@@ -246,8 +246,33 @@
   <div class="brand-fixed" aria-hidden="true"><img alt=""></div>
 
   <!-- ===== 센티넬: API BASE + 브릿지 주입 위치 (멱등) ===== -->
-  <!-- KOBO-SENTINEL:orchmon-api-base -->
-<script>window.ORCHMON_API_BASE='http://localhost:5193/api/orchmon'; window.ORCHMON_FORCE_OFFLINE=false;</script>
+  <!-- KOBO-SENTINEL:ak7-api-base -->
+<script>
+/* AK7 sentinel — single source of truth (DEV:5181, MOCK:5191)
+   - You can override via query: ?mode=DEV or ?mode=MOCK
+   - Or via localStorage: AK7_API_BASE
+*/
+(function(){
+  try{
+    var Q = new URLSearchParams(location.search);
+    var mode = (Q.get('mode')||localStorage.getItem('AK7_MODE')||'MOCK').toUpperCase();
+    if(mode!=='DEV' && mode!=='MOCK') mode='MOCK';
+    var defBase = (mode==='DEV')? 'http://localhost:5181/api/ak7' : 'http://localhost:5191/api/ak7';
+    var base = (window.AK7_API_BASE || localStorage.getItem('AK7_API_BASE') || defBase).replace(/\/+$/,'');
+    window.AK7_MODE = mode;
+    window.AK7_API_BASE = base;
+    /* Back-compat for existing boosters */
+    window.ORCHMON_API_BASE = base;
+    window.ORCHMON_FORCE_OFFLINE = false;
+    try{
+      localStorage.setItem('AK7_MODE', mode);
+      localStorage.setItem('AK7_API_BASE', base);
+      localStorage.setItem('ORCHMON_API_BASE', base);
+    }catch(e){}
+    console.log('[AK7] MODE='+mode,'BASE='+base);
+  }catch(e){ console.warn('[AK7] sentinel init failed', e); }
+})();
+</script>
   <script>
     /* API Base 규격(DEV=5183 / MOCK=5193). 필요 시 여기만 수정. */
     window.ORCHMON_API_BASE = window.ORCHMON_API_BASE || "http://localhost:5183/api/orchmon";


--- /mnt/data/ak7_src/webui/public/booster/overview-cards.js
+++ /mnt/data/ak7_patched/webui/public/booster/overview-cards.js
@@ -63,8 +63,8 @@
   box.innerHTML = `
     <table class="orch-table orch-small">
       <tr><td>현재 API</td><td class="orch-mono"><span id="sApi">${API||'—'}</span></td></tr>
-      <tr><td>DEV (5183)</td><td><i id="sDev" class="orch-led"></i> <span class="orch-mono">http://localhost:5183</span></td></tr>
-      <tr><td>MOCK (5193)</td><td><i id="sMock" class="orch-led"></i> <span class="orch-mono">http://localhost:5193</span></td></tr>
+      <tr><td>DEV (5181)</td><td><i id="sDev" class="orch-led"></i> <span class="orch-mono">http://localhost:5181</span></td></tr>
+      <tr><td>MOCK (5191)</td><td><i id="sMock" class="orch-led"></i> <span class="orch-mono">http://localhost:5191</span></td></tr>
     </table>
     <div class="orch-small">아래 버튼으로 /health 점검</div>`;
   host.appendChild(box);
@@ -74,7 +74,7 @@
     catch{ led.className='orch-led err'; } };
 
   const healthBtn = $('#healthBtn', host) || (()=>{ const b=document.createElement('button'); b.className='btn'; b.textContent='/health 점검'; host.appendChild(b); return b; })();
-  const run = ()=>{ ping('http://localhost:5183', $('#sDev',box)); ping('http://localhost:5193', $('#sMock',box)); };
+  const run = ()=>{ ping('http://localhost:5181', $('#sDev',box)); ping('http://localhost:5191', $('#sMock',box)); };
   healthBtn.onclick = run; run();
 })();
 


--- /mnt/data/ak7_src/webui/public/booster/settings-pane.js
+++ /mnt/data/ak7_patched/webui/public/booster/settings-pane.js
@@ -3,10 +3,10 @@
 const echo = document.querySelector('#apiBaseEcho');
 
 const $ = (s,p=document)=>p.querySelector(s);
-const API_DEF = 'http://localhost:5193/api/orchmon';
+const API_DEF = 'http://localhost:5191/api/ak7';
 function getAPIBase(){ const ls = localStorage.getItem('ORCHMON_API_BASE'); return (window.ORCHMON_API_BASE || ls || API_DEF); }
 function setAPIBase(v){ window.ORCHMON_API_BASE = v; localStorage.setItem('ORCHMON_API_BASE', v); echo && (echo.textContent = 'API Base: '+v); }
-function getOriginFromBase(base){ return (base||'').replace(/\/api\/orchmon\/?$/,'') || location.origin; }
+function getOriginFromBase(base){ return (base||'').replace(/\/api\/ak7\/?$/,'') || location.origin; }
 async function testHealth(base){ const origin = getOriginFromBase(base); const url = origin + '/health'; const t0 = performance.now();
   try{ const r = await fetch(url,{cache:'no-store'}); const ms=Math.round(performance.now()-t0); if(!r.ok) throw new Error('HTTP '+r.status); const j=await r.json(); return {ok:true,ms,json:j,url}; }
   catch(e){ const ms=Math.round(performance.now()-t0); return {ok:false,ms,err:String(e),url}; } }
@@ -39,7 +39,7 @@
   <div class="set-h"><h6>API Base & 모드</h6><span class="set-badge" data-k="health">-</span></div>
   <div class="set-row">
     <label>API Base</label>
-    <input type="text" data-k="apibase" placeholder="http://host:port/api/orchmon">
+    <input type="text" data-k="apibase" placeholder="http://host:port/api/ak7">
     <button class="set-btn" data-k="probe">/health 점검</button>
     <button class="set-btn" data-k="apply">적용</button>
     <button class="set-btn" data-k="apply-reload">적용+새로고침</button>


--- /mnt/data/ak7_src/webui/public/booster/errkb-cards.js
+++ /mnt/data/ak7_patched/webui/public/booster/errkb-cards.js
@@ -124,7 +124,7 @@
 if(window.ORCHBUS && typeof ORCHBUS.subscribe==='function'){ ORCHBUS.subscribe(onEvent); }
 else{
   const S=[ /* sample 15 */ 
-    {type:'error',role:'server',action:'listen',msg:'Error: listen EACCES: permission denied 0.0.0.0:5193',ts:Date.now()-6e5},
+    {type:'error',role:'server',action:'listen',msg:'Error: listen EACCES: permission denied 0.0.0.0:5191',ts:Date.now()-6e5},
     {type:'error',role:'server',action:'listen',msg:'Error: listen EACCES: permission denied 0.0.0.0:5188',ts:Date.now()-54e4},
     {type:'error',role:'server',action:'docker',msg:'manifest for mcr.microsoft.com/windows/node:lts-windowsservercore-ltsc2022 not found',ts:Date.now()-48e4},
     {type:'error',role:'server',action:'docker',msg:'no matching manifest for windows(10.0.19045)/amd64 in the manifest list entries',ts:Date.now()-42e4},
@@ -132,12 +132,12 @@
     {type:'error',role:'npm',action:'node-pty',msg:'EPERM: operation not permitted, rmdir ...\\node_modules\\node-pty\\deps',ts:Date.now()-30e4},
     {type:'error',role:'ps',action:'parser',msg:"ParserError: The '<' operator is reserved for future use.",ts:Date.now()-24e4},
     {type:'error',role:'ps',action:'var',msg:"The variable '$Date.now()' cannot be retrieved because it has not been set.",ts:Date.now()-18e4},
-    {type:'error',role:'web',action:'fetch',msg:'GET http://localhost:5183/health net::ERR_CONNECTION_REFUSED',ts:Date.now()-12e4},
+    {type:'error',role:'web',action:'fetch',msg:'GET http://localhost:5181/health net::ERR_CONNECTION_REFUSED',ts:Date.now()-12e4},
     {type:'error',role:'web',action:'static',msg:'net::ERR_FILE_NOT_FOUND /public/img/STS_LOGO-크림색.png',ts:Date.now()-60e3},
     {type:'info',role:'host',action:'health',msg:'{\"ok\":true,\"service\":\"orchmon\",\"mode\":\"MOCK\"}',ts:Date.now()-40e3},
     {type:'action',role:'input',action:'good',msg:'user pressed GOOD',ts:Date.now()-30e3},
-    {type:'error',role:'api',action:'timeline',msg:'GET /api/orchmon/timeline 404 (Not Found)',ts:Date.now()-20e3},
-    {type:'error',role:'api',action:'apibase',msg:'http://localhost:5193/api/orchmon : not found',ts:Date.now()-10e3},
+    {type:'error',role:'api',action:'timeline',msg:'GET /api/ak7/timeline 404 (Not Found)',ts:Date.now()-20e3},
+    {type:'error',role:'api',action:'apibase',msg:'http://localhost:5193/api/ak7 : not found',ts:Date.now()-10e3},
     {type:'error',role:'web',action:'health',msg:'[ORCHMON] health: ERROR Failed to fetch',ts:Date.now()-5e3},
   ]; S.forEach(onEvent);
 }


--- /mnt/data/ak7_src/webui/public/booster/orch-bus-auto.js
+++ /mnt/data/ak7_patched/webui/public/booster/orch-bus-auto.js
@@ -1,11 +1,11 @@
 (()=>{ if(window.ORCHBUS){ console.log("[ORCHBUS] already present"); return; }
-const DEF='http://localhost:5193/api/orchmon';
-if(!window.ORCHMON_API_BASE || !/\/api\/orchmon$/.test(window.ORCHMON_API_BASE)){
+const DEF='http://localhost:5191/api/ak7';
+if(!window.ORCHMON_API_BASE || !/\/api\/ak7$/.test(window.ORCHMON_API_BASE)){
   window.ORCHMON_API_BASE = DEF;
   console.log("[ORCHBUS] ORCHMON_API_BASE set →", window.ORCHMON_API_BASE);
 }
 const API    = window.ORCHMON_API_BASE.replace(/\/$/,'');
-const ORIGIN = API.replace(/\/api\/orchmon$/,'') || location.origin;
+const ORIGIN = API.replace(/\/api\/ak7$/,'') || location.origin;
 const CANDS  = [ API + "/timeline", ORIGIN + "/timeline" ];
 
 const subs = new Set(); let es=null, idx=0, backoff=1000, currentUrl=null;


